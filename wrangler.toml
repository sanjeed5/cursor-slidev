# Must match the Worker name in Cloudflare dashboard (Workers & Pages).
name = "cursor-slidev"
compatibility_date = "2024-01-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
